package elementary;

public class RandomConnection {
	
	public static void main(String[] args)
	{/*
		int N = 10;
		double p = 1;
		
		double[] pointX = new double[N];
		double[] pointY = new double[N];
		
		double basicArc = 2 * Math.PI / N;
		StdDraw.setCanvasSize(800, 800);
		
		for(int i = 0 ; i < N ; i++)
		{
			pointX[i] = 0.5 + 0.3 * Math.cos(basicArc * i);
			pointY[i] = 0.5 + 0.3 * Math.sin(basicArc * i);
			StdDraw.filledCircle(pointX[i] ,pointY[i], 0.005);
		}
		
		for(int i = 0 ; i < N; i++)
		{
			for(int j = i + 1 ; j < N ; j++)
			{
				if(StdRandom.bernoulli(p))
				{
					StdDraw.line(pointX[i], pointY[i], pointX[j], pointY[j]);
				}
			}
		}*/
	}
}
